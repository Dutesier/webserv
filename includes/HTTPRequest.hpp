#ifndef HTTP_REQUEST_HPP
#define HTTP_REQUEST_HPP

#include <string>

namespace webserv {

struct HTTPRequest {

        /* Constructor */
        HTTPRequest(std::string req);
};

} // namespace webserv

#endif /* HTTP_REQUEST_HPP */
